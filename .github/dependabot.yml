# Keep GitHub Actions pinned to immutable SHAs.
# Dependabot updates the SHA and the human-readable tag comment together.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
